{{!-- Language picker hidden for now: the translations are still the
      original JavaScript lessons, not the Rust conversion. Restore the
      picker by reverting this file (see git history). --}}
